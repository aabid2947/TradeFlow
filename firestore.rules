rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary open rules for testing chat functionality
    // ⚠️ WARNING: This allows all read/write access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
